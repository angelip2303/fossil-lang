let input = csv!("tests/RMLTC0002a-CSV/student.csv")

type Output(subject: string) do
    @rdf(uri = "http://xmlns.com/foaf/0.1/name")
    Name: string
end

input
|> fn row -> Output("http://example.com/${row.Name}") { Name = row.Name } end
|> Rdf.serialize("tests/RMLTC0002a-CSV/actual.nq")
