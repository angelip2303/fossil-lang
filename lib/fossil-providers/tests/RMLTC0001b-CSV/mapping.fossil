let input = csv!("tests/RMLTC0001b-CSV/student.csv")

type Output(subject: string) do
    @rdf(uri = "http://xmlns.com/foaf/0.1/name")
    Name: string
end

input
|> fn row -> Output("_:${row.Name}") { Name = row.Name } end
|> Rdf.serialize("tests/RMLTC0001b-CSV/actual.nq")
