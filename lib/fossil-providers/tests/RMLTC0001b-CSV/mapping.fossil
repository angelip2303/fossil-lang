type Input = csv!("tests/RMLTC0001b-CSV/student.csv")

type Output = {
    #[rdf(uri = "http://xmlns.com/foaf/0.1/name")]
    Name: string,
}

Input::load()
|> map(fn(row) -> Output {
    @id = "_:${row.Name}",
    Name = row.Name
})
|> Rdf::serialize("tests/RMLTC0001b-CSV/actual.nq")
