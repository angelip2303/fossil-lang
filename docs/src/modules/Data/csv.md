# Data.Csv

The `Data.Csv` module provides functions for working with CSV files.
It includes functions for reading and writing CSV data.

## Reading CSV Data

```
open Data.Csv
let result = read "path/to/file.csv"
```

## Writing CSV Data

```
open Data.Csv
let result = ...
write result "path/to/file.csv"
```
